import styled from "styled-components";
import { colors } from "../../styles";

export const Section = styled.section`
margin-top: 0px;
max-height: ${colors.heightMain};
overflow-y: scroll;
max-width: 700px;
margin: auto;
`